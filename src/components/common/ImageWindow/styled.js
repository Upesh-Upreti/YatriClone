import styled from 'styled-components';

const StyledImageWindow = styled.div`
    height: 100% ;
    .image-wrapper{
        position: relative;
        height: 100% ;
        width: 100%;
    }
    .logo{
        padding: 3rem ;
    }
`

export default StyledImageWindow